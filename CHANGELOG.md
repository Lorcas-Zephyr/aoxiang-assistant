# 更新日志

## 2.1.3 - 2026-08-16

- 修复后台自动更新完成后，已打开的应用仍保留旧内存数据的问题；成绩、课表和电费保存成功后会立即刷新首页及对应页面，从后台返回时也会补查遗漏的更新。
- 修复本地成绩或课表被清空后，重新同步完整数据不发送变化通知的问题；恢复的数据会按全部新增课程生成成绩或排课更新通知。
- 为 GPA、剩余电费、GPA 与电费组合及首页数据概览的 6 种规格补充系统小组件选择器预览图，并标注对应尺寸。

## 2.1.2 - 2026-08-16

- 新增 GPA、剩余电费、GPA 与电费组合、首页数据概览桌面小组件，覆盖 `1×1`、`2×1`、`4×1` 和 `2×2` 尺寸；成绩、电费、主题及登录状态变化后会同步刷新。
- 首页数据概览宽版由 `4×2` 调整为 `4×1`，保留四项数据的纵向指标样式；修正 `1×1` 剩余电费组件数值偏右的问题。
- 课表地点包含“友谊”时，按课程日期使用友谊校区作息：`5月1日-9月30日` 使用夏季时间，`10月1日-4月30日` 使用冬季时间。
- 首页今日课程、周课表课程块和桌面课表小组件显示对应校区的准确时间，小组件的课程结束判断也使用对应校区作息。
- 当周全部课程均在友谊校区时，周课表左侧节次时间同步切换为友谊校区；混合校区时保留长安时间轴，并由每个课程块显示自身时间。

## 2.1.1 - 2026-08-16

- 移除自动更新页无效的“检查并授权”按钮；从系统设置返回后立即原位刷新可检测的权限状态，自启动项改为“无法检测，请自行确认”。
- 登录流程在每轮处理、页面完成和历史地址变化时同步检查当前地址；除主动退出或切换账号外不再清除认证 Cookie，并在登录或采集成功后立即持久化会话，避免后续采集重新触发短信风控。
- 成绩、课表或电费采集遇到登录失效、短信验证码或安全验证时，统一切换到可见的统一认证页面；人工认证通过后保留同一会话，并按该采集项原有的网页显示设置自动继续抓取。

## 2.1.0 - 2026-08-16

- 修复检查更新时 GitCode 空描述字段被显示为 `null` 的问题，改用包含完整 Release 正文的接口并过滤无效空值。
- 课表同步优先读取教务系统结构化数据，兼容不同账户的课表页面加载时机和模板差异；学期开始日期改为读取网页“学期起始日期”。
- 自动读取课表页全部学期并按真实起始日期锁定当前课表；学期周数和结束日期由最后一门课计算且至少为两周，只有超过结束日期才读取下一学期，空课表也会保留。
- 设置页新增“手动更新”二级页，可分别控制电费、成绩和课表手动采集时是否显示网页，后台自动更新始终隐藏；修复课表页正文被误判为访问错误后主动跳回教务首页的问题，被教务系统实际退回时会有限重试并显示明确状态。
- 自动更新新增后台权限检查与分步授权，包含通知、精确定时唤醒、忽略电池优化及厂商自启动/后台启动设置；应用退到后台后由系统闹钟和后台服务接管，进程退出后仍保留下一次更新。
- 后台权限状态改为原位刷新，解决点击“检查并授权”和从系统设置返回时的整页重绘闪烁。
- 放宽登录成功判定：实际进入 `uis.nwpu.edu.cn` 后只要跳出该认证站点即可通过，也兼容直接进入任意有效教务学生页面，不再限定精确的 `/student/home` 地址；隐藏密码框不再被误判为登录页，只有网页明确返回账号或密码错误时才计为登录失败。

## 2.0.0 - 2026-08-16

- 首次登录和切换账号使用 App 账号密码弹窗；连续两次密码登录失败后显示校方统一认证页面，通过后再保存密码。
- 只有认证后实际进入教务系统首页 `https://jwxt.nwpu.edu.cn/student/home` 才判定登录成功，避免中间跳转被误判。
- 修复账号、短信验证码等自绘弹窗四角仍露出矩形阴影的问题。
- 设置页新增启动页面选择，可在首页、课表、成绩、管理和设置之间选择，通知及小组件跳转仍保持原目标页面。
- 更新成绩时继续读取成绩表，并从学生画像页单独读取 GPA；加权平均分仍按课程成绩和学分在本地计算。
- 修复学生画像中“数值在前、个人 GPA 在后”导致 GPA 一直读取的问题；画像读取失败时最多等待 15 秒，随后保存成绩并保留原 GPA。
- 同一课程存在重修记录时只保留分数最高的一条，课程数量、平均分和更新通知均使用去重后的数据。
- 首页、成绩、课表和管理页在数据更新完成后立即原位刷新，替换页面时保留旧内容直到新内容就绪，避免空白闪烁。
- 应用内更新按钮直接打开对应版本的 GitCode APK 下载地址，不再跳转到发行版列表。
- 登录成功、切换账号和退出登录后立即刷新账号页状态，不再等待后台数据同步或重新进入页面。

## 1.9.1 - 2026-08-16

- 修复切换账号及手动更新成绩、课表、电费时，密码或验证码错误没有明确提示的问题。
- 登录验证完成后在后台依次同步成绩、课表和电费，不再显示采集页面。
- 统一采集状态为“正在登录”“正在打开目标页面”“正在读取数据”三个阶段。
- 每天 00:00–01:00 暂停电费更新，手动操作时提示电费系统正在结算，并在 01:00 后恢复自动更新。

## 1.9.0 - 2026-08-15

- 设置页保存账号前验证学号和翱翔门户密码，密码错误、验证码错误或验证码失效时给出明确提示。
- 切换账号时先完整退出并清除旧账号数据，再登录和同步新账号。
- 首次登录成功后依次同步成绩、课表和电费，再恢复后台定时更新。
- 修复窄屏设备上成绩页绩点文字错位的问题。
- 启动时显示用户群号 `450804497`，支持选择不再提示，并在关于页长期显示。
- 每次打开应用时从 GitCode 检查新版本，关于页也可手动检查；支持查看更新内容、更新或跳过当前版本。
- GitHub 与 GitCode 后续保持同步发布。

## 1.8.1 - 2026-08-14

- 修复七天课表中地点文字被截断、无法显示完整教室号的问题。
- 课表单节高度按当前学期全部课程的最大内容需求统一计算；内容可容纳时保持默认高度。

## 1.8 - 2026-08-14

- 按每个排课时段保存教师和完整上课地点，课表点击显示当前时段信息，课程管理汇总显示全部教师和地点。
- 支持同一括号内的多段周次，例如 `2~3,11周`。
- 移除课程管理中的新增、编辑和删除入口。
- 重绘课程详情、登录和短信验证码弹窗，统一圆角、阴影和居中布局。

## 1.7 - 2026-08-14

- 限制课表周视图在第一周和最后一周之间翻页，避免越界生成重复周。
- 限制月历在学期覆盖的首月和末月之间翻页。
- 修正越界滑动后内部偏移继续累积的问题，反向滑动始终只移动一页。

## 1.6 - 2026-08-14

- 优化课表左右滑动识别，降低被纵向滚动拦截的概率。
- 根据松手时的最终位移决定是否翻页，反向拖回或滑动距离不足时平滑回到当前页面。
- 限制课表翻页拖动范围，并适配宽屏设备的触发距离。

## 1.5 - 2026-08-14

- 修复课表周/月视图翻页时的全页重绘闪烁，手势翻页和顶部按钮翻页均直接切换相邻页面。
- 优化周/月切换、今日按钮和学期切换，仅更新课表内容区域，减少不必要的页面重建。
- 保持现有页面和底部导航状态，避免切换标签页时出现白屏或闪烁。

## 1.4 - 2026-08-14

- 课表周视图支持左右滑动切换上一周、下一周，月历支持左右滑动切换月份。
- 滑动课表时同步拉入相邻周/月，修复拖动过程中的闪烁和空白页面问题，同时保留纵向滚动与课程点击。
- 修复圆角按钮阴影使用尖角轮廓的问题，统一文字按钮和步进按钮的圆角阴影。
- 优化桌面小组件选择器预览，显示今日/今明课表示例和组件尺寸标注。

## 1.3 - 2026-08-13

- 新增课表桌面小组件：2 列显示今天，4 列显示今天和明天，支持独立点击、空白区域进入课表和课程过多时滚动查看。
- 修复小组件大小布局复用问题，确保 2 列组件不会错误显示双日课表。
- 修复手动设置学期开始日期的周次计算：所选日期会自动调整为当日或之前最近的周一。
- 兼容迁移已有学期数据，后台自动更新不会覆盖手动设置的学期起始周。
- 月视图课程改为使用课程颜色圆点显示，组件和课表数据变化后自动刷新。

## 1.2 - 2026-08-13

- 重新设计首页、课表、成绩、管理和设置界面，统一排版、图标与 `5dp` 圆角。
- 设置页改为二级面板，并新增关于页面。
- 新增成绩和课表变化通知开关，通知会列出发生变化的课程。
- 自动更新改为系统定时唤醒和后台采集，并支持分钟、小时、天三种单位。
- 自动更新间隔支持直接输入 `1–999`，同时保留加减按钮。
- 修正主题色选择区和电费阈值输入框的布局。
- 首页成绩和电费更新入口增加文字标识，避免两个同步按钮无法区分。
- 优化七天课表、课程时间、退出登录数据清理及手动更新入口。

## 1.1 - 2026-08-12

- 首个公开版本。
- 支持统一身份认证、课表、成绩、宿舍电费和本地课表管理。
