<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_small_row_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="24dp"
    android:orientation="horizontal"
    android:background="@drawable/widget_row_background"
    android:padding="5dp">
    <TextView android:id="@+id/widget_dot" android:layout_width="14dp" android:layout_height="wrap_content" android:text="●" android:textSize="10sp" android:gravity="center_vertical" />
    <LinearLayout android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1" android:orientation="vertical">
        <TextView android:id="@+id/widget_name" android:layout_width="match_parent" android:layout_height="wrap_content" android:maxLines="1" android:ellipsize="end" android:textColor="#19324D" android:textSize="11sp" android:textStyle="bold" />
        <TextView android:id="@+id/widget_time" android:layout_width="match_parent" android:layout_height="wrap_content" android:maxLines="1" android:ellipsize="end" android:textColor="#5E7185" android:textSize="10sp" />
        <TextView android:id="@+id/widget_location" android:layout_width="match_parent" android:layout_height="wrap_content" android:maxLines="1" android:ellipsize="end" android:textColor="#5E7185" android:textSize="10sp" />
    </LinearLayout>
    <TextView
        android:id="@+id/widget_spacer"
        android:layout_width="1dp"
        android:layout_height="96dp"
        android:visibility="gone"
        android:text="" />
</LinearLayout>
